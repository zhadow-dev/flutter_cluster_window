#include <gtest/gtest.h>

// Placeholder test. The real testing is done in Dart.
// This ensures the CMake test target builds.
namespace flutter_cluster_window {
namespace test {

TEST(FlutterClusterWindowPlugin, PluginExists) {
  // Just verify the test infrastructure works.
  EXPECT_TRUE(true);
}

}  // namespace test
}  // namespace flutter_cluster_window
